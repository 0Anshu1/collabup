specVersion: "v1beta"
serviceId: "collabup"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "collabup-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
